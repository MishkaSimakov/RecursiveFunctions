// This intricate program checks if two numbers are equal
// prints 1 if they are and 0 otherwise

main: () -> void = {
    a: i64 = 42;
    b: i64 = 37;

    if (a == b) {
        print(1);
    } else {
        print(0);
    }
}