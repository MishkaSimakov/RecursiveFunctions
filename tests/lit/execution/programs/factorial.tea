// RUN: %execute "%s" | %FileCheck %s

// CHECK:       1
// CHECK:       1
// CHECK-NEXT:  2
// CHECK-NEXT:  6
// CHECK-NEXT:  24
// CHECK-NEXT:  120
// CHECK-NEXT:  720

import "io"

factorial: (n: i64) -> i64 = {
    if (n == 0) {
        return 1;
    }

    return factorial(n - 1) * n;
}

main: () -> i64 = {
    println_i64(factorial(0));
    println_i64(factorial(1));
    println_i64(factorial(2));
    println_i64(factorial(3));
    println_i64(factorial(4));
    println_i64(factorial(5));
    println_i64(factorial(6));

    return 0;
}