// RUN: %execute "%s" | %FileCheck %s

// CHECK:       123
// CHECK-NEXT:  321

import "io"

main: () -> i64 = {
    tuple: (i64, i64);

    tuple.0 = 123;
    tuple.1 = 321;

    println_i64(tuple.0);
    println_i64(tuple.1);

    return 0;
}