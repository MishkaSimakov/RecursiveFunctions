// RUN: %execute "%s" | %FileCheck %s

// CHECK:       123
// CHECK-NEXT:  321

extern println: (value: i64) -> ()

main: () -> i64 = {
    tuple: (i64, i64);

    tuple.0 = 123;
    tuple.1 = 321;

    println(tuple.0);
    println(tuple.1);

    return 0;
}