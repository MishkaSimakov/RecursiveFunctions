1
2
3
4
5
6
//
output1: (tuple: (i64,)) -> () = {
    output(tuple.0);
}

output2: (tuple: (i64, i64)) -> () = {
    output(tuple.0);
    output(tuple.1);
}

output3: (tuple: (i64, i64, i64)) -> () = {
    output(tuple.0);
    output(tuple.1);
    output(tuple.2);
}

main: () -> i64 = {
    tuple1: (i64,) = (1,);
    tuple2: (i64, i64) = (2, 3);
    tuple3: (i64, i64, i64) = (4, 5, 6);

    output1(tuple1);
    output2(tuple2);
    output3(tuple3);

    return 0;
}