144
//
math: namespace = {
    sqr: (value: i64) -> i64 = {
        return value * value;
    }
}

main: () -> i64 = {
    value: i64 = 12;
    squared: i64 = math::sqr(value);

    output(squared);

    return 0;
}