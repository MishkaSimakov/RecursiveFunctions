1
2
3
4
1
2
3
4
//
get_tuple: () -> (i64, i64, i64, i64) = {
    return (1, 2, 3, 4);
}

get_tuple2: () -> (i64, i64, i64, i64) = {
    return get_tuple();
}

main: () -> i64 = {
    tuple: (i64, i64, i64, i64) = get_tuple();

    output(tuple.0);
    output(tuple.1);
    output(tuple.2);
    output(tuple.3);

    tuple2: (i64, i64, i64, i64) = get_tuple2();

    output(tuple2.0);
    output(tuple2.1);
    output(tuple2.2);
    output(tuple2.3);

    return 0;
}